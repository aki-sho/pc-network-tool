# PC Network Tool

PC Network Tool は、Windows 上で現在のネットワーク設定や接続状態を確認するためのデスクトップアプリです。  
Electron を使用し、HTML・CSS・JavaScript ベースで構成しています。

PC のネットワーク設定を行う前や、VPN 接続、仮想マシン接続、クラウド接続の確認前に、まず自分の環境がどうなっているかを把握することを目的としています。

## バージョン

v1.0.0

## 主な機能

### 1. アダプタ一覧の表示
現在の PC に存在するネットワークアダプタを一覧表示します。

表示例:
- イーサネット
- Wi-Fi
- ループバック
- Bluetooth ネットワーク接続 など

### 2. IPv4 / IPv6 の表示
各アダプタに割り当てられている IPv4 / IPv6 アドレスを確認できます。

### 3. ゲートウェイの確認
接続先ネットワークのデフォルトゲートウェイ情報を確認できます。

### 4. DNS の確認
各アダプタに設定されている DNS 情報を確認できます。

### 5. MAC アドレスの表示
各アダプタの MAC アドレスを表示します。

### 6. DHCP / 固定 IP の確認
IP アドレスが DHCP で取得されているか、固定 IP 設定かを確認できます。

### 7. パブリック IP の取得
現在インターネット側から見えているパブリック IP を表示します。

### 8. 接続中の基本情報の表示
現在の接続情報を一覧で表示します。

表示例:
- Protocol
- Local Address
- Local Port
- Remote Address
- Remote Port
- State
- PID
- Process

## 利用目的

このアプリは、次のような確認を行いたい場面を想定しています。

- ネットワーク設定前に現在の状態を確認したい
- VPN 接続前後の変化を確認したい
- 仮想マシン接続時にどの IP やアダプタを見ればよいか確認したい
- ローカル環境やクラウド接続前に、通信状態を整理したい
- ポート待受や接続状況をまとめて見たい

## 動作環境

- Windows
- Node.js
- npm

## 使用技術

- Electron
- HTML
- CSS
- JavaScript
- systeminformation

## インストール

依存関係をインストールします。

npm install

## 起動方法

開発用に起動します。

npm start

## ビルド方法

Windows 向けにビルドします。

npm run build

## 現在のバージョンで確認できる項目

- アダプタ一覧
- IPv4 / IPv6
- ゲートウェイ
- DNS
- MAC
- DHCP / 固定 IP
- パブリック IP
- 接続中の基本情報

## 補足

- パブリック IP の取得にはインターネット接続が必要です
- 環境によっては一部の項目が取得できない場合があります
- 管理者権限や Windows の設定状態により表示内容が変わる場合があります

## 今後の拡張候補

- VPN 状態の専用表示
- 仮想アダプタの識別強化
- ルーティング情報の表示
- ポートの絞り込み機能
- 接続状態の色分け表示
- スナップショット保存機能
- 接続前後の差分比較機能

## ライセンス

MIT